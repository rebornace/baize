# 开源首版边界：已交付 / 后续 / 企业定制

> 状态：**已确认**（2026-08-28）  
> 目的：在做生产硬化（F）之前，列出「方向上未做」的功能点，供产品确认是否有任一项需要挤进**开源首个生产版**。  
> 依据：架构草案、已落地规格 defer、本轮头脑风暴（多订阅与官方 SDK 不定入开源首版）。

**确认结论（2026-08-28）：**

| 决策 | 项 |
|------|-----|
| **本版要开发** | **I1** Inbox → HITL resume；**P4** PostgreSQL 存储驱动；**微信 Channel（iLink）+ 会话归属**（规格已批准） |
| **本版不开发、改文档** | **P1** SDK 表述；**P2** 工作流=线性流水线表述 |
| **本版不做** | **P3** OTel；A1 多订阅；B1 SDK 实现；其余 §3/§4 默认否 |
| **本分支实现中** | **X1** MCP 导出（规格已批准，`feat/mcp-export-v0`） |
| **排队（新需求）** | X2 `/ui` 切大模型；X3 中间件多源 |
| **本版之后** | **F** 生产硬化（含 P1/P2 文档收口） |

---

## 0. 开源首版已齐（对照，不重复开发）

| 方向 | 代表能力 |
|------|----------|
| Runtime | ReAct、HITL、SQLite、控制面 Gate |
| 连接 | OpenAPI / HTTP 插件 / MCP、工具目录、登录捕获 |
| 运营入口 | Chat UI、会话持久化、回滚/Fork、Skills、附件、分析/报告页 |
| 集成闭环 | Inbox v1、出站 Webhook、出站重试/死信、线性 workflow、callback_urls、企业执行回调 |
| 工程 | CI、跨平台启动、示例与 README 生产集成小节 |

---

## 1. 不开源首版（已定「否」）

| ID | 功能点 | 说明 | 建议归属 | 是否需要本版上线 |
|----|--------|------|----------|------------------|
| A1 | 出站 Webhook **多订阅** | 维护面大；可用自建网关 fan-out | 后续 / 企业定制 | **否** |
| B1 | 官方 **TS / Python SDK**（实现） | Webhook + 自带 UI 即可 | 后续 / 企业定制 | **否** |

---

## 2. 架构清单 vs 现状（已确认）

| ID | 功能点 | 现状 | 归属 | 是否需要本版上线 |
|----|--------|------|------|------------------|
| P1 | 架构写「开源含 SDK」 | 无 SDK 包 | **改文档**（后续/可选/企业） | **改文档即可** |
| P2 | 架构写完整工作流状态机 | 仅线性 `workflow.yaml` | **改文档**（开源 DSL=线性） | **改文档即可** |
| P3 | 可选 **OTel** | 未实现 | 后续（运维向） | **否** |
| P4 | 存储可换 **PostgreSQL** | 仅 SQLite / Memory | **本版开发** | **是** |
| P5 | 多 Agent 协作 | 未做 | 后期 / 企业 | **否** |
| P6 | 企业 Memory 插件 | 未产品化 | 商业 / 企业 | **否** |
| P7 | 企微 / 钉钉原生 Channel 样板 | 网关转发即可 | 样板后续 / 企业 | **否** |

---

## 3. 规格 defer（已确认）

| ID | 功能点 | 来源 | 归属 | 是否需要本版上线 |
|----|--------|------|------|------------------|
| I1 | Inbox 入站触发 **HITL resume** | Inbox v1.1 | **本版开发** | **是** |
| I2 | Inbox 附件 / 多模态入站 | Inbox v1.1 | 开源后续 | **否** |
| I3 | Inbox JSONPath / 模板映射 | Inbox v1.1 | 网关 / 后期 | **否** |
| I4 | Inbox 限速多副本一致 | Inbox v1.1 | 部署 / 后期 | **否** |
| I5 | 多 Channel 广播 / 路由引擎 | YAGNI | 不做 | **否** |
| W1 | Workflow 分支 / 循环 / 表达式 | workflow 非目标 | 开源后续 | **否** |
| W2 | Workflow 可视化编辑器 | 商业侧 | 企业 / 商业 | **否** |
| C1 | golangci-lint / eslint 等 | CI v0 非目标 | 工程后续（可随 F） | **否** |
| U1 | 对话自动生成标题 | 会话规格 | 体验后续 | **否** |
| U2 | Playwright 全量 E2E | Demo B | 工程后续 | **否** |

---

## 4. 商业侧（默认不进开源首版）

| ID | 功能点 | 是否需要本版上线 |
|----|--------|------------------|
| E1–E5 | 多租户/SSO、厂商连接器、画布、托管记忆、企业定制 SDK/多订阅等 | **否** |

---

## 5. 本版执行顺序（待拍板先后）

本版要开发的两项彼此独立，各需单独规格 → 计划 → 实现；**不能塞进同一个实现计划**。

| 顺序 | 里程碑 | 状态 |
|------|--------|------|
| **1** | **I1** Inbox → HITL resume | **已交付** |
| **2** | **P4** PostgreSQL / SQL 驱动 | **已完成** |
| **2b** | **微信 Channel（iLink）+ 会话归属** | **已交付**（`feat/channel-weixin-v0`；规格 `specs/2026-08-29-channel-weixin-v0-design.md`） |
| **3** | **X1** MCP 导出（规格 `specs/2026-08-29-mcp-export-v0-design.md`）→ X2 / X3 | **本分支实现中**（`feat/mcp-export-v0`） |
| **4** | P1/P2 文档 + **F** 生产硬化 | 上述之后 |

---

*本文件仅存在于 baize_real（`docs/superpowers`）；不进入 public 开源导出。*
