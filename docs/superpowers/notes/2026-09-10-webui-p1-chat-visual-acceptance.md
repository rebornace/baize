# P1 聊天页 · 设计视觉验收对照表

> 对应设计稿：`docs/superpowers/specs/2026-09-09-webui-experience-refresh-design.md`（§3 设计语言、§6 聊天体验）
> 日期：2026-09-10
> 范围：**仅聊天页**（设置页 IA/人话化属 P2/P3，不在本轮）
> 方法：隔离实例浏览器实测（计算样式/DOM 度量 + 明/暗 × 桌面/手机截图），不靠主观。
> 判定：✅ 达标 · ⚠️ 已修但需真机复核 · ❌ 未达标

## A. 设计令牌与一致性（设计 §3.1）

| # | 验收项（可度量） | 现状（修复前） | 结果 |
|---|---|---|---|
| A1 | 主色唯一：聊天壳层不出现第二套蓝（令牌 `--accent` 浅色 `#2563eb` / 暗色 `#3b82f6`） | 侧栏选中/焦点环用了 `#1f6feb` 第二套蓝 | ✅ 全改 `--accent` / `--accent-soft` / `--focus-ring` |
| A2 | 聊天可见区域无硬编码色值（遮罩/透明渐变/iframe 内部除外） | 28 处裸色，聊天区含 `#f6f7f9`/`#c5cad3`/`rgba(...)` | ✅ 聊天区仅余透明渐变；代码块底色走新令牌 |
| A3 | 焦点环统一令牌，明暗各一套 | 多处 `rgba(37,99,235,.22)` 写死 | ✅ 新增 `--focus-ring`（明 .35 / 暗 .4） |
| A4 | 暗色模式聊天区无浅色块穿帮（程序化扫描亮度>200 的底） | 扫描通过 | 待复测 |

## B. 间距 / 圆角 / 字号刻度（设计 §3.1：4px 基准、圆角 控件8–10/卡片14/气泡12–16/胶囊999）

| # | 验收项 | 修复前 | 结果 |
|---|---|---|---|
| B1 | 间距只用 4/8/12/16/24/32（`--space-*`），不再出现 8.8/10.4/13.6 等分数像素 | 大量 `0.55/0.65/0.85rem` → 分数 px | ✅ 聊天壳层归 `--space-*` |
| B2 | 字号落在统一阶（正文 15px=0.9375rem，辅助 13px=0.8125rem，小字 12px） | 15.2 / 15.52 / 0.97rem 等非刻度值 | ✅ 消息正文 15px、辅助 13px |
| B3 | 用户气泡圆角 ∈ 12–16 | 18.4px（`1.15rem`） | ✅ 改 `--radius-xl`(16) |
| B4 | 输入框圆角 ∈ 12–16；主按钮胶囊 | 18.4px | ✅ 输入框 16，发送 999 |
| B5 | 控件小圆角 8–10（侧栏项/范围按钮/输入/附件） | 6.4px 等 | ✅ 归 `--radius-sm`(8) |

## C. 明确视觉 bug

| # | 验收项 | 修复前 | 结果 |
|---|---|---|---|
| C1 | 空状态「添加模型」芯片横排胶囊，不被压扁、不竖排 | 34×87px 四字竖排，composer 撑高到 130px | ✅ `flex:0 0 auto + nowrap`，32px 高 |
| C2 | 空状态 composer 高度与有模型时一致（≈单行高度） | 130px | 待复测 |
| C3 | 用户/助手消息行高、气泡留白舒展 | 行高未约束 | ✅ 用户 1.6 / 助手 1.65 |

## D. 聊天体验（设计 §6，P1 已做，回归确认）

| # | 验收项 | 结果 |
|---|---|---|
| D1 | 模型为输入框内小芯片；无模型时「添加模型」不破坏布局 | 待复测 |
| D2 | 欢迎区克制：一句问候 + 一句产品语，无示例问题/Token 措辞 | ✅ 保持 |
| D3 | 助手消息高频操作（复制/重新回答）常驻 hover；低频收「⋯」菜单 | ✅ 保持 |
| D4 | HITL/工具/工作流人话卡（本轮不改逻辑，仅确认样式未被破坏） | 待复测 |
| D5 | 拒绝审批显示「已按你的选择停止本次操作。」，无 failed 英文 | ✅（上一提交） |

## E. 响应式与可访问性（设计 §3.4、§7）

| # | 验收项 | 结果 |
|---|---|---|
| E1 | ≤768px 侧栏变抽屉、输入区不被芯片/附件挤占 | 待真机/窄视口截图 |
| E2 | 触摸设备消息操作常显（`@media (hover:none)`） | 规则在，待真机 |
| E3 | 正文对比度 ≥4.5:1（明/暗） | 待度量 |

## F. 工程结构（设计 §3.1、§8）

| # | 验收项 | 现状 | 结果 |
|---|---|---|---|
| F1 | 聊天样式从 29.7KB 混合 `style.css` 抽离为 `styles/chat.css`，`main.tsx` 单一入口 | `chat.css` 缺失 | 待定（纯搬移、零渲染变化后做） |

## 复测矩阵（截图留档）

| 场景 | 浅色 | 暗色 |
|---|---|---|
| 桌面·空会话（含无模型芯片） | ☐ | ☐ |
| 桌面·普通对话 | ☐ | ☐ |
| 桌面·工具/HITL 对话 | ☐ | ☐ |
| 手机 390px·抽屉+输入区 | ☐ | ☐ |
