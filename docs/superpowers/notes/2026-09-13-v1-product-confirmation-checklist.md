# 开源第一版：产品需求确认清单（全量盘点）

> 日期：2026-09-13  
> 用途：产品确认「第一版还要做什么 / 明确不做」  
> 依据：`2026-08-28-oss-backlog`、`2026-09-13-spec-ledger`、各规格非目标/defer、近期 DoD 核验  
> 状态：待产品勾选

**用法：** 对 **A 组**每一项勾：`首版要做` / `首版不做（后续）` / `改文档即可`。  
B 组为历史已否决项（可复审）。C 组为已交付（勿重复立项）。

---

## A. 待产品确认（首版是否还要做）

### A1. 文档与叙事（成本低，对外可见）

| ID | 项 | 现状 | 建议默认 |
|----|-----|------|----------|
| **DOC-P1** | 架构/对外文档去掉「开源含 TS/Python SDK」承诺 | `docs/architecture-and-plugin-protocol.md` 仍写 SDK | **首版要做（改文档）** |
| **DOC-P2** | 工作流对外表述对齐「仅线性流水线」，勿暗示完整状态机/分支 | architecture 混有「可选状态机」旧句 + 线性 workflow 节 | **首版要做（改文档）** |
| **DOC-README** | 用户路径术语与设置导航人话持续一致（P4 已做过一轮） | 大体已齐 | 抽查即可，非新功能 |

### A2. 生产硬化 F（需另开规格切片）

| ID | 项 | 说明 | 建议默认 |
|----|-----|------|----------|
| **F** | 生产硬化总包 | 尚无已批准 F 切片规格；常含安全默认、健康检查、配置校验、密钥/口令运维、部署清单等 | **首版要做，但须先定切片**（否则范围会漂） |
| **F-C1** | golangci-lint / eslint 等工程门禁 | OSS backlog C1，可挂在 F | 随 F 定 |

### A3. WebUI / 体验债（功能多半已有）

| ID | 项 | 说明 | 建议默认 |
|----|-----|------|----------|
| **UI-RUNTIME** | 运行参数设置页人话化 | 热更新 API/页已有，壳偏旧（无完整 PageHeader 范式） | **可选**（体验，非功能缺口） |
| **UI-MCP-OAUTH** | 远程 MCP OAuth 2.1 交互登录 | P3-C 明确不做；现仅静态请求头 | **首版不做**（独立大项） |
| **UI-SKILL-EDITOR** | 技能可视化编辑器 | P3-D 明确不做 | **首版不做** |
| **UI-AT-LOGIN** | 登录入口 `@` 直达等 | P3-D 明确不做 | **首版不做** |
| **UI-E2E** | Playwright 全量 E2E | U2 / 多规格非目标 | **首版不做**（工程后续） |
| **UI-KV-EDITOR** | 请求头/环境变量结构化行编辑器 | P3-C 非目标；现 Textarea | **首版不做** |
| **UI-EXPORT-DB** | `export_db_readonly` 的 UI | 仅 YAML/bootstrap | **首版不做** |

### A4. 渠道 / 适配器增强

| ID | 项 | 说明 | 建议默认 |
|----|-----|------|----------|
| **CH-SDK** | 公共 Go SDK（插件解耦阶段三） | 规格暂缓；无外部 Go 插件刚需前不做 | **首版不做** |
| **CH-OUTBOX** | 渠道出站持久化 outbox（跨重启可靠投递） | 2A 非目标；现同步+有限重试 | **首版不做** |
| **CH-PORT** | 适配器动态端口发现 | 现固定/配置端口 | **首版不做** |
| **CH-FEISHU** | 飞书/钉钉/企微**真实**适配器样板 | P7；现有通用 webhook + `examples/im-adapter` | **首版不做**（机制已齐） |
| **CH-GROUP** | 微信群聊 | **产品已确认不要**；`@chatroom` 丢弃 | **首版不做**（已定） |
| **CH-WL-FORCE** | 微信白名单「强制」安全特性 | runtime 规格曾 defer；webhook 入站已有 allowlist 测试 | **待核验**：若现状已满足运营白名单，则勾「不做」 |

### A5. Inbox / 集成增强

| ID | 项 | 说明 | 建议默认 |
|----|-----|------|----------|
| **I2** | Inbox 附件 / 多模态入站 | OSS 否 | **首版不做** |
| **I3** | Inbox JSONPath / 模板映射 | OSS 否 | **首版不做** |
| **I4** | Inbox 限速多副本一致 | OSS 否 | **首版不做** |
| **I5** | 多 Channel 广播 / 路由引擎 | YAGNI | **首版不做** |
| **A1** | 出站 Webhook 多订阅 | OSS 否 | **首版不做** |

### A6. 平台能力扩展

| ID | 项 | 说明 | 建议默认 |
|----|-----|------|----------|
| **B1** | 官方 TS/Python SDK **实现** | 与 DOC-P1 配套：文档改掉承诺即可 | **首版不做** |
| **P3** | OpenTelemetry | OSS 否 | **首版不做** |
| **P5** | 多 Agent 协作 | OSS 否 | **首版不做** |
| **P6** | 企业 Memory 插件产品化 | 商业 | **首版不做** |
| **W1** | Workflow 分支/循环/表达式 | OSS 否 | **首版不做** |
| **W2** | Workflow 可视化画布 | 商业 | **首版不做** |
| **BLOB-EXT** | Connector 规格 / Skill 包进 BlobStore | Blob v0 非目标 | **首版不做** |
| **U1** | 对话自动生成标题 | OSS 否 | **首版不做** |
| **MCP-LOGIN** | MCP 工具「需本人登录」/capture | 后端无链路 | **首版不做** |
| **MCP-LEAK** | DELETE MCP 连接器不关 stdio 池（已知后端债） | P3-C 记录 | **可选挂 F** |
| **RT-HOT** | 存储/Redis/S3 驱动热切换、端口 TLS 热改等 | runtime 非目标 | **首版不做** |
| **SEC-KV** | 凭据 KV at-rest 加密 | runtime 非目标 | **可选挂 F** |

### A7. 商业侧（默认不进开源首版）

| ID | 项 | 建议默认 |
|----|-----|----------|
| **E1–E5** | 多租户/SSO、厂商连接器、画布、托管记忆、企业 SDK/多订阅等 | **首版不做** |

---

## B. 历史已确认「本版不做」（可复审，默认保持否）

见 `2026-08-28-oss-backlog` §1–§4：A1、B1、P3、P5–P7、I2–I5、W1–W2、C1、U1–U2、E1–E5。  
**微信群聊**（2026-09-13 产品确认）：不做。

---

## C. 第一版已交付（勿再立项）

Runtime / HITL / SQLite+PG / Gate / OpenAPI·HTTP 插件·MCP / 工具目录 / 登录捕获 / Chat UI / 会话 / 回滚 Fork / Skills / 附件 / 分析页 / Inbox+HITL resume / 出站 Webhook+重试 / 线性 workflow / 回调 / MCP 导出 / 多模型 / 上下文压缩 / 中间件多源 / Blob / 会话工作区 / 渠道注册表 / webhook 渠道 / 微信适配器+supervisor / 运行参数热更新（功能）/ WebUI P0–P4 + 消息组人话化与结果导向文案。

细节目录：`2026-09-13-spec-ledger.md`。

---

## D. 建议的「第一版收口」最小集（供拍板）

若目标是「可对外称开源生产可用切片」，建议默认只强留：

1. **DOC-P1 + DOC-P2**（改公开架构叙事）  
2. **F**（先头脑风暴定 1～2 个硬化切片，避免无穷大）  
3. **UI-RUNTIME**（可选，体验一致性）  

其余 A 组默认 **首版不做**，除非你明确勾选上调。

---

## E. 请产品回复格式（示例）

```
DOC-P1: 首版要做
DOC-P2: 首版要做
F: 首版要做（切片另开头脑风暴）
UI-RUNTIME: 首版要做 | 首版不做
CH-WL-FORCE: 首版不做（白名单现状够用）
其余 A 组: 维持「首版不做」
```

确认后按勾选项开规格/计划，不再混进已否决项。
