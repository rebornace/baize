# 本地密钥（勿提交）。复制为 .env 后填写。
BAIZE_API_KEY=
# 对接真实 OpenAPI 服务时的 JWT（可写裸 token，或带 Bearer 前缀）；作 connector Headers 兜底
BAIZE_CONNECTOR_TOKEN=
# 设置项秘密落库主密钥（AES）。生产必填；未设置则拒写 api_key/口令/inbox secret，且无法启动已加密库。
BAIZE_SETTINGS_KEY=
# Runtime HTTP 监听（覆盖 YAML listen；改后需重启）
# BAIZE_LISTEN=:8080
